create database if not exists sistema_restaurante;
use sistema_restaurante;
-- hola mundo

create table if not exists una_tabla(
    id_campo int not null auto_increment,
    nombre_campo varchar(45) not null
);
